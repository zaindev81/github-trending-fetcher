rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to trending data
    match /trending/{language} {
      allow read: if true;
      // Only allow writes from Firebase Functions
      allow write: if false;
    }
  }
}
